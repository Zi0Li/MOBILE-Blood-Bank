import 'package:flutter/material.dart';

class GiveCredtiPage extends StatefulWidget {
  const GiveCredtiPage({super.key});

  @override
  State<GiveCredtiPage> createState() => _GiveCredtiPageState();
}

class _GiveCredtiPageState extends State<GiveCredtiPage> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}